import { InjectionToken } from '@angular/core';
export interface AngularIcoComponentConfig {
    baseUrl?: string;
    cache?: boolean;
}
export declare const ICO_CONFIGURATION: InjectionToken<AngularIcoComponentConfig>;
